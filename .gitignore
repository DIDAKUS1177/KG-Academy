# dependencias
node_modules/

# build
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts

# base de datos local (se regenera con: npm run setup)
prisma/*.db
prisma/*.db-journal

# variables de entorno (contienen secretos)
# copiar .env.example a .env para trabajar en local
.env
.env.local
.env.production

# material fuente de los cursos (exports de Genially, 20+ MB cada uno).
# El contenido se sirve embebido por URL publica; estos exports son solo
# respaldo local y no deben versionarse.
cursos/

# python (generadores de documentos)
__pycache__/
*.pyc

# sistema y logs
*.log
.DS_Store
Thumbs.db
